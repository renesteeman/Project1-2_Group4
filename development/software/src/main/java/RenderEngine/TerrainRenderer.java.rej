diff a/GameEngine2/src/main/java/RenderEngine/TerrainRenderer.java b/GameEngine2/src/main/java/RenderEngine/TerrainRenderer.java	(rejected hunks)
@@ -57,7 +57,7 @@
     }
 
     private void loadModelMatrix(Terrain terrain){
-        Matrix4f transformationMatrix = Maths.createTransformationMatrix(new Vector3f(terrain.getX(), 0, terrain.getZ()), 0, 0, 0, 1);
+        Matrix4f transformationMatrix = Maths.createTransformationMatrix(new Vector3f(terrain.getxStart(), 0, terrain.getzStart()), 0, 0, 0, 1);
         shader.loadTransformationMatrix(transformationMatrix);
     }
 
