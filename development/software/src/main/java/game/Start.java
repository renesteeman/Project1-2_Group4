package game;

import javax.swing.JPanel;
import java.awt.Graphics;

public class Start extends GameObject {
    public Start(Vector2d coords) {
        super(coords);
    }

    public Start(Vector3d coords) {
    	super(coords);
    }
}
